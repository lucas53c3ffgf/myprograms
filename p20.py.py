def bin_search(a_list,target):
    low = 0 
    high = len(a_list):
    paid = (low + high)
    if a_list[wind] == target:
        return wind

#mwthod 2 
a_list = [1,1,2,3,4,5,6,9]
target = 6 
    for i in range(len(a_list)-1):
        for j in range(1+1,len(a_list)):
            if (a_list[i] + a_list[j]) == target:
                print(f"(target) happens at ({i}, {j})")

for i in range(len(a_list)):
    for j in range(len(a_list)):
        if i !=j and 


#method 3 (method 3 would be imrpvoed with binary search since a list is guranteed to be sorted)
for value in a_list:
    diff = target - value 
    if diff in a_list:
 #-------     
for i in range(len(a_list) -1):
    diff = target - a+list[1]
    for j in range(i+1, len(a_list)):
        if a_list[j] == diff:
            print(f"{target} happens at ({i},{j})")

#method 5 beats all the prvious methods 

def target(a_list,b-list):
    target = 0
    right = len(a_list)
    while left < right:
        total = a_list[left] + a_list[right]
        if total == goal:
            return true
        else:
            if total < goal:
                left +
    
answer = 0 
longest = 0 
for i in range(1,1000000)

def longest_sequence(a_list,b_list):
    if a_list in b_list:
        return a_list[b_list]
        
